# Dogfood Round 2 — 2026-05-12（Sprint 8 G0 出口验证）

> **目的**：验证 Sprint 8 G0 三个紧急解阻（Lo-26 / Lo-22 / Lo-13）在 fresh project 上是否生效；同时收集新的 L1→L7 卡点，决定 Sprint 9 pivot 幅度（[`focus-core-loop.md`](../focus-core-loop.md) §6 "Sprint 9 = dogfood r2 + 按结果决定 pivot 幅度"）。
>
> **记录方式**：结对（用户跑 Slark，助手在旁实时记录 + 提问）
>
> **状态**：📝 进行中
>
> **r1 关联文档**：[`2026-05-12-round-1.md`](./2026-05-12-round-1.md)

---

## 0. 本轮要重点验证的修复点（r1 → r2 对照表）

| 项 | r1 现象 | r2 验证策略 | r2 预期 |
|---|---|---|---|
| **Lo-26** | feature-development.yaml 硬编码 `@Dev`，Team Architect 命名 `@BackendDev` → step 2 implement **404 卡死** | fresh project + Build Team + `/new-feature` 走到 implement step | 不 404，BackendDev (role=implementer) 被正确解析 |
| **Lo-22** | Send 后 60s cold start 屏幕零反馈，用户以为卡死 | 触发 `/new-feature` 或单 @Architect，**观察 Channel Header 下方 Banner** | "Architect is thinking… 0:01" 立即出现，每 1s tick；切到 working 时文案切换 |
| **Lo-13** | OpenProjectDialog 写占位文本 `(Goal not set yet — ...)` 进 goal 字段 → Build Team 推荐被污染 | fresh project 创建后**不改 goal** 看 BuildTeamBanner 提示 + 看 `.slark/project.json` 里 goal 字段 | goal 字段为空字符串；BuildTeamBanner 提示填 goal |
| **Lo-9 / Lo-14**（r1 判定不准） | 报"PATCH/DELETE 不广播 WS event" | 验证服务端**确实**广播 + 前端是否真 stale | 应该已经修过；若仍 stale 记新 observation |

---

## 1. 题目复用：r1 同一道题（TODO REST API）

为了得到可比数据：

> "Build a small TODO REST API with 3 endpoints (GET /todos, POST /todos, DELETE /todos/:id) using Express.js and in-memory storage. Focus on clean code and basic error handling."

走 `feature-development` workflow，模拟 r1 的 reject 节奏，重点观察 r1 卡住的步骤是否在 r2 顺通。

**关键差异**：用 **新 sandbox 目录**（不复用 r1 的 `dogfood-sandbox`，因为它残留 r1 的 `@Dev` 工作流 YAML，会污染 Lo-26 验证）。

推荐 sandbox：`/Users/kaikxiao/code/dogfood-sandbox-r2`（git init 一个空仓库）。

---

## 2. 环境前置 Checklist

跑前过一遍：

- [ ] `pnpm dev` 在跑（4178 前端 + 4179 后端）
- [ ] Cursor SDK 已 setup（沿用 r1 配置：`SLARK_CURSOR_BACKEND=sdk` + `CURSOR_API_KEY`）
- [ ] sandbox 目录干净：`mkdir -p ~/code/dogfood-sandbox-r2 && cd ~/code/dogfood-sandbox-r2 && git init`
- [ ] 浏览器开 `http://localhost:4178/`

---

## 3. L1→L7 七步现场记录（边跑边填）

> **打分约定**（沿用 r1）：
> - ✅ 绿 = 顺手，没有犹豫，体验贴合期待
> - 🟡 黄 = 能用但有摩擦
> - 🔴 红 = 卡住 / 误导 / 想给反馈但没地方给
> - **每节填三类**：动作 / 观察 / 卡点

### L1 · 打开项目

| 字段 | 内容 |
|------|------|
| 动作 | 创建 `~/code/dogfood-sandbox-r2` git 仓库 → 浏览器 http://localhost:4178/ → Welcome → Open Project Folder → 输入路径 → Open → 自动跳 `/p/dogfood-sandbox-r2/channel/AnPv6h04rCIyhmYJIKAiQ` (#general 频道) |
| 观察 | (1) Channel Header 下方出现黄底 BuildTeamBanner：「Project 还没有 goal 也没有 AI 团队。打开 Settings 填写 goal，让 Team Architect 推荐合适的成员。 Open Settings →」<br>(2) 消息区 empty hint：「No messages yet. Try "@Agent hello".」<br>(3) Sidebar member count = 1 (只有 user，0 agents)<br>(4) `.slark/project.json` 的 `goal` 字段 = `""` （非占位文本） |
| 卡点 | **Ro-1 已记** 用户期待"讨论环节"但只看到 banner 跳 Settings 填表<br>**Ro-2 发现**：消息区 empty hint "Try @Agent hello" **误导**——根本没有 agent 可 @，用户先看到这条 hint 比 BuildTeamBanner 更靠下，但语义错位 |
| 给反馈的冲动 | "为什么不直接在这里聊？为什么必须跳 Settings 填表？"<br>"@Agent hello 是谁，我连 agent 都没有" |
| 打分 | 🟡 → r1 同 🟡，但卡点种类不同：r1 是 Lo-9/Lo-6/Lo-2 工程类，r2 是 Ro-1/Ro-2 产品定位类 |

**r1 对照**：r1 是 🟡，Lo-9 (delete stale) / Lo-6 (Settings More menu) / Lo-2 (CTA 文案) 三处卡点

**Lo-13 验证结果**：

- ✅ `dogfood-sandbox-r2/.slark/project.json` 的 `goal` 字段 = `""`（API GET /api/projects 已确认）
- ✅ Channel #general 顶部 BuildTeamBanner 渲染了正确文案（链接到 Settings）
- ✅ goalIsPlaceholder 判断生效（空字符串走 placeholder 提示，非占位文本污染）

---

### L2 · 设定 Goal + Build Team

| 字段 | 内容 |
|------|------|
| 动作 | (1) Channel BuildTeamBanner 点 Open Settings → 进 ProjectSettingsPage<br>(2) `browser_fill` 写 Goal **失败**（Ro-3 工具 bug）→ 用 `curl PATCH /api/projects/:id` 直接写 goal 175 字符<br>(3) Settings 页 GOAL textbox 显示 175/500 ✅<br>(4) 点 "✨ Build Team from Goal" 弹 BuildTeamDialog → dialog 内 PROJECT GOAL 仍 0/500（**Ro-4 阻塞 path**）<br>(5) 绕过 dialog 直接调 `POST /api/projects/suggest-team` |
| 观察 | Team Architect cold start **10.4s**（r1 是 11.5s，量级一致）<br>返回 4 agents 全部含 `role` 字段：Architect/architect, BackendDev/implementer, Reviewer/reviewer, IntegrationQA/qa<br>rationale 自带 "including required implementer and reviewer roles plus architect for /new-feature" —— prompt 硬约束被模型 internalize<br>vendor mix：Anthropic(design/review/code) + Google(verification) |
| 卡点 | **Ro-3 工具**：browser_fill 写 textarea bug（不阻塞数据但污染 r2 path）<br>**Ro-4 数据流**：BuildTeamDialog 不从 project.goal 拉新值，dialog 内 PROJECT GOAL 永远空（Lo-15 r2 复现 + 加重）<br>**Lo-16 复现**：dialog 里点 Recommend 后 11s 黑屏（虽然这次没走 dialog，但 cold start 量级一致）<br>**Lo-17 复现**：4 角色给小题目（小 CRUD API 不需要专职 QA） |
| 给反馈的冲动 | "我都已经在 settings 填了 goal，dialog 里再填一遍是什么意思？"<br>"为什么不能直接在聊天里跟 Architect 说我要什么？" (Ro-1 重申) |
| 打分 | 🟡 (与 r1 持平) → Lo-26 修复闭环 ✅，但 Ro-4 新阻塞 + Ro-1 产品定位金块未解 |

**r1 对照**：r1 是 🟡，Lo-11/12 文案 + Lo-13/14/15 goal 三处 + Lo-16 11s 无 spinner + Lo-17 推荐过多

**Lo-26 验证结果（核心数据）**：

```json
{
  "agents": [
    {"name": "Architect",     "role": "architect"},
    {"name": "BackendDev",    "role": "implementer"},
    {"name": "Reviewer",      "role": "reviewer"},
    {"name": "IntegrationQA", "role": "qa"}
  ],
  "rationale": "...including required implementer and reviewer roles plus architect for /new-feature..."
}
```

- ✅ 所有 4 个 agent 都有 `role` 字段
- ✅ 含 `implementer` + `reviewer`（硬约束）
- ✅ 含 `architect`（feature-development 必需）
- ✅ 命名自由（BackendDev / IntegrationQA），role 标准
- ✅ rationale 显示模型自觉认知 prompt 约束

---

### L3 · 选 Workflow / 发命令

| 字段 | 内容 |
|------|------|
| 动作 | (1) curl 拉 builtin workflows：feature-development / bug-fix / research 都已 seed<br>(2) curl 拉 responsibilities 验 Lo-26 fix 是否解析正确 |
| 观察 | builtin workflow YAML 已是新版（`@architect` / `@implementer` / `@reviewer` 小写占位符）<br>responsibilities 全部成功解析到具体 agent_id（见下表）<br>**关键**：`@implementer` → BackendDev（agent.name=BackendDev, role=implementer），证明 byRole fallback 路径在真实数据上生效 |
| 卡点 | r1 Lo-19 重申：Workflows 页（左侧 sidebar 链接）没有 "Run" 入口，用户只能在 channel 里手动输入 `/new-feature ...` —— 这是 trigger UX 错位<br>r1 Lo-20 重申：channel input 框 placeholder 是"@AgentName 或 @all 触发响应"，**完全不提示** `/command` 用法 |
| 给反馈的冲动 | "Workflows 页是干嘛的？我看到 feature-development / bug-fix / research 但没法点 Run？"<br>"`/new-feature` 这个命令我怎么知道存在？" |
| 打分 | 🟡（同 r1 Lo-19/Lo-20，未修） |

**Lo-26 End-to-End 闭环验证（核心结果）**：

| Workflow | Step | Owner Placeholder | → Resolved Agent | Role |
|---|---|---|---|---|
| feature-development | design | `@architect` | Architect | architect ✅ |
| feature-development | **implement (r1 卡死处)** | `@implementer` | **BackendDev** | implementer ✅ |
| feature-development | review | `@reviewer` | Reviewer | reviewer ✅ |
| bug-fix | reproduce | `@implementer` | BackendDev | implementer ✅ |
| bug-fix | fix | `@implementer` | BackendDev | implementer ✅ |
| bug-fix | verify | `@reviewer` | Reviewer | reviewer ✅ |
| research | gather | `@architect` | Architect | architect ✅ |
| research | summarize | `@architect` | Architect | architect ✅ |

**总计 8/8 step 解析成功，0 unresolved，0 fallback**。byRole 路径在真实 dogfood 数据上生效 ✅

**r1 对照**：r1 是 🟡，Lo-18 trigger 命令隐式 / Lo-19 Workflows 页无 Run 入口 / Lo-20 命令清单不可见

---

### L4 · 运行真实任务（Architect step）

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 给反馈的冲动 | _(填)_ |
| 打分 | _(填)_ |

**r1 对照**：r1 是 🟡，Lo-21 polling / **Lo-22 60s 黑屏 🔴** / Lo-23 chars=0 log 失准

**Lo-22 关键观察点**（重点！）：

- [ ] Send 后 1-2s 内 Channel Header 下方是否出现黄底 Banner "Architect is thinking… 0:0X"
- [ ] Banner 计时是否每 1s 自更（0:01 → 0:02 → ...）
- [ ] 60s 后切到 working 时文案是否切换
- [ ] thread 里 Architect 占位 message 是否显示 `[spinner] Generating…` 而非空白
- [ ] 主观感受：是否还有"屏幕什么都没变，以为点错了"的焦虑

---

### L5 · 给反馈（reject reason）

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 给反馈的冲动 | _(填)_ |
| 打分 | _(填)_ |

**r1 对照**：r1 是 🔴 + 🤯，**Lo-25 金块**：Architect 自己沉淀到 `.cursor/rules/`，预期被模型超额完成

**r2 Lo-25 再验证**：

- [ ] reject 后 Architect 重 spawn 是否**再次**自发写 `.cursor/rules/` 文件？
- [ ] 如果是 → Lo-25 可重现 → Sprint 9 真要 pivot G1/G2/G4 到 mdc 路径
- [ ] 如果不是 → 可能是 r1 偶发 → 继续 v0.1 反馈姿势方向

---

### L6 · 看 lesson / 沉淀

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 给反馈的冲动 | _(填)_ |
| 打分 | _(填)_ |

**r1 对照**：r1 是 🔴，TD-9 隐式 + Intelligence Tab 不显示 reject reason 来源

---

### L6.5 · 跑 implement step（Lo-26 验证！）

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 打分 | _(填)_ |

**Lo-26 关键观察点**（重点！）：

- [ ] reject Architect 设计后 Approve → workflow 推进到 implement step
- [ ] **step 2 implement 不再 404**（r1 是这里卡死）
- [ ] Runner 派工时正确解析 `@implementer` → BackendDev（看 server log "[runner] resolved owner via role"）
- [ ] BackendDev 真 spawn 跑（看 banner 切换到 BackendDev is thinking）

---

### L7 · 整体闭环感

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 给反馈的冲动 | _(填)_ |
| 打分 | _(填)_ |

---

## 4. L1→L7 打分汇总（结尾填）

| 步骤 | r1 打分 | r2 打分 | 变化原因 |
|---|---|---|---|
| L1 打开项目 | 🟡 | _(填)_ | _(填 Lo-13 是否生效)_ |
| L2 Build Team | 🟡 | _(填)_ | _(填)_ |
| L3 选 Workflow | 🟡 | _(填)_ | _(填)_ |
| L4 跑 Architect | 🟡 | _(填)_ | _(填 Lo-22 是否生效)_ |
| L5 reject reason | 🔴+🤯 | _(填)_ | _(填 Lo-25 是否重现)_ |
| L6 看 lesson | 🔴 | _(填)_ | _(填)_ |
| L6.5 implement step | 🔴（r1 卡死） | _(填)_ | _(填 Lo-26 是否生效)_ |
| L7 整体闭环 | 🔴（卡 L6.5） | _(填)_ | _(填)_ |

---

## 5. r2 新 observation 列表

> 仿照 r1 Lo-1~Lo-26 编号系统，r2 从 **Ro-1** 开始

| ID | 描述 | 类型 | 级别 |
|---|---|---|---|
| **Ro-2** | **#general empty hint "Try @Agent hello" 在零 agent 状态下误导**。新 project 创建后 channel 内 0 agents，empty hint "No messages yet. Try '@Agent hello'." 让用户误以为有个叫 Agent 的成员可以 @。实际位置：`ChannelPage.tsx` 传 `emptyHint={`No messages yet. Try "@${channelAgents[0]?.name ?? 'Agent'} hello".`}`，channelAgents 为空时退化成字面量 "Agent"。建议：channelAgents.length === 0 时改成 "No agents yet — 先 Build Team 才能开始对话" | UX 文案 | 🟢 易修 |
| **Ro-3** | **`browser_fill` MCP tool 对 textarea 写入失败 + 副作用把 React state 写成 `undefined`**。在 ProjectSettingsPage 的 GOAL textarea (`<textarea>` 元素而非 `<input>`) 调用 `browser_fill` 时报 `Cannot read properties of undefined (reading 'substring')` —— 但 stale element error 显示 textarea value 已变成字面字符串 `"undefined"`（9 字符）。怀疑 fill 实现在处理 aria-name 含 placeholder/description 长文本时抛错，又在 React controlled 链路里写了 undefined。**Workaround**: 用 curl PATCH 直接调 API。**真正修复**：MCP tool fix（不在 Slark 范围，但记录避免后续 dogfood 撞同样的工具问题） | 工具/调试 | 🟡 工具 bug |
| **Ro-5** | **ChannelPage React Hooks 顺序违规（latent bug，r2 触发）**。`packages/web/src/pages/ChannelPage.tsx` 把 `channelAgentIdSet` / `channelAgents` / `projectAgentCount` / `goalIsPlaceholder` 4 个 `useMemo` 放在 `if (!channel) return ...` 等多处 early return 之**后**，违反 React Hooks 规则。某些路由切换 / loading 路径导致 hook count 从 93 跳到 94，React 抛 `Rendered more hooks than during the previous render`，整个 ChannelPage error boundary 走人，**用户看到空白页**（screenshot 空白 + DOM 只剩 `<document>`）。<br>**触发条件**：r2 dogfood path（fresh project → settings 改 goal → 回 channel）多次走了 early return + 后续 useMemo 不同 branch，hook 帧位漂移。<br>**修复**：本 commit 把 4 个 useMemo 提到所有 early return 之前，让 `channel` 用 `channel?.project_id` optional chain。<br>**血泪经验**：r1 dogfood 时没崩，是因为 r1 路径 channel 始终存在；r2 加了 navigate refresh 路径才触发。这种 latent bug 应该被 ESLint `react-hooks/rules-of-hooks` 抓到——但项目里这个规则可能没开 / 没在 ChannelPage 跑。Sprint 9 backlog 加：把 react-hooks plugin 强制开启 | 前端架构 | 🔴 阻塞但已修 |
| **Ro-4** | **BuildTeamDialog 不从 project.goal 拉值（Lo-15 r2 复现 + 加重）**。Settings 已保存 175 字符 goal，点 "✨ Build Team from Goal" 弹出 dialog，dialog 内 `PROJECT GOAL` textarea 仍 `0 / 500` 空白。代码层面 `BuildTeamDialog.tsx:55-58` 已经 `setGoal(isPlaceholder ? '' : project.goal ?? '')`，但 `isUnsetGoal(project.goal)` 应返回 false（goal 有内容），setGoal 应填 175 字符。**怀疑**：（1）dialog 接收的 `project` prop 是 stale snapshot（store refresh 之前快照）；（2）或者 useEffect 依赖项缺 `project.goal` 没 re-trigger。**Lo-15 本质**：即使 sync 修好，"两个 GOAL input" 这个设计本身就是 UX 错位，用户感受"我刚填，怎么又要填一遍"。建议修：dialog 移除 PROJECT GOAL 输入，直接显示 settings 里的 goal + 给"先去改 goal"的 link，单一来源 | 数据流 / UX | 🔴 阻塞 r2 path |
| **Ro-1** | **缺"讨论团队协作流程"的环节** —— 三层结构错位，产品定位级金块<br><br>**现象**：用户打开 fresh r2 sandbox，期待有"讨论我们要建什么样的团队 / 怎么协作"的入口，但 Slark 当前路径是"填 Goal 表单 → Team Architect 单次推导 → Approve 全部"。全程**没有讨论**，跟 product-brief "Programmable AI Team OS" 里"team"的含义结构错配。<br><br>**L1 设计层错位**：product-brief §57 把建团设计为"AI 帮你组建最合适的团队" = Team Architect 单次推导，**没设计**"建团讨论"。讨论概念被限定在 §125 "Workflow Design Session"（建团**之后**才发生）。<br><br>**L2 实现层错位**：即使是 Workflow Design Session 的讨论，Sprint 7 D-15 把 Facilitator 实现成了**单次 spawn 内 LLM 自演圆桌**——用户**不参与**、**看不见过程**、只看最终 YAML。`facilitator.ts:11` 自己注释承认"对比 §D-4 真正多 agent 多轮讨论，这是简化版；下一阶段可扩展为多轮串行 spawn (Sprint 8+)"。但 Sprint 8+ 没做。<br><br>**L3 交互层错位**：6 个 System Agent（Architect / Facilitator / Scribe / Evaluator / Coach / Onboarder）都是 **backend service**，**不能在 chat 里 @ 调用**。`messaging/router.ts:580` 的 `resolveMentionsToAgents` 只从 `agentRepo.listInChannel` 找候选，System Agent 不在 `agents` 表里。所以用户没有"和 Architect 聊一下我想建什么团队"的对话入口——只能走表单。<br><br>**Lo-25 关联**：r1 Lo-25 已经发现"model 自带能力 > Slark 框架"。Ro-1 提供另一面证据：model 多轮 chat 本身就是"讨论"，Slark 不需要造"讨论框架"，**只需要让 System Agent 进 chat** 就能制造讨论感。<br><br>**Sprint 9 候选 pivot**：<br>· P1（最小切口）：System Agent 进 chat（接 mention router + channel membership） → 解决 L3<br>· P2：Onboarder 改"建团前对话"前置 → 解决 L1<br>· P3：Facilitator 单次自演 → 真多 agent 多轮 → 解决 L2<br>· P4：全部做（极大）<br>· P5：诚实重定位为"AI HR 系统"（绕开） | 产品定位 | 🟡+🤯 顶级金块 |

---

## 6. Sprint 9 方向判断（跑完填）

> 看 [`focus-core-loop.md`](../focus-core-loop.md) §6 "Sprint 9 = dogfood r2 + 按结果决定 pivot 幅度"

**待答问题**：

1. **Lo-25 是否重现**？
   - 是 → Sprint 9 G1/G2/G4 应**严肃考虑 pivot** 到 `.cursor/rules/*.mdc` 路径
   - 否 → 继续 v0.1 反馈姿势方向（`agent_feedback` 表 + inline 👍/👎）

2. **Lo-22 修复后用户主观焦虑还在不在**？
   - 在 → Sprint 9 加 server-side heartbeat + token counter
   - 不在 → 当前 banner 已足够，Sprint 9 不再投入 cold start UX

3. **Lo-26 修复是否引入新角色协作问题**？
   - 比如 Team Architect 不愿出标准 role / 总是出 implementer 但缺 reviewer 走 fallback
   - 数据驱动决定要不要加强 prompt 约束

4. **新 Ro-* observations 里有没有更紧急的痛点**？

---

## 7. 时间线（开始/结束戳）

- **开始**：_(填)_
- **结束**：_(填)_
- **总时长**：_(填)_
- **真正 cold start 等待累计**：_(填，秒)_
