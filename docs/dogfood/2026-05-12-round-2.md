# Dogfood Round 2 — 2026-05-12（Sprint 8 G0 出口验证）

> **目的**：验证 Sprint 8 G0 三个紧急解阻（Lo-26 / Lo-22 / Lo-13）在 fresh project 上是否生效；同时收集新的 L1→L7 卡点，决定 Sprint 9 pivot 幅度（[`focus-core-loop.md`](../focus-core-loop.md) §6 "Sprint 9 = dogfood r2 + 按结果决定 pivot 幅度"）。
>
> **记录方式**：结对（用户跑 Slark，助手在旁实时记录 + 提问）
>
> **状态**：📝 进行中
>
> **r1 关联文档**：[`2026-05-12-round-1.md`](./2026-05-12-round-1.md)

---

## 0. 本轮要重点验证的修复点（r1 → r2 对照表）

| 项 | r1 现象 | r2 验证策略 | r2 预期 |
|---|---|---|---|
| **Lo-26** | feature-development.yaml 硬编码 `@Dev`，Team Architect 命名 `@BackendDev` → step 2 implement **404 卡死** | fresh project + Build Team + `/new-feature` 走到 implement step | 不 404，BackendDev (role=implementer) 被正确解析 |
| **Lo-22** | Send 后 60s cold start 屏幕零反馈，用户以为卡死 | 触发 `/new-feature` 或单 @Architect，**观察 Channel Header 下方 Banner** | "Architect is thinking… 0:01" 立即出现，每 1s tick；切到 working 时文案切换 |
| **Lo-13** | OpenProjectDialog 写占位文本 `(Goal not set yet — ...)` 进 goal 字段 → Build Team 推荐被污染 | fresh project 创建后**不改 goal** 看 BuildTeamBanner 提示 + 看 `.slark/project.json` 里 goal 字段 | goal 字段为空字符串；BuildTeamBanner 提示填 goal |
| **Lo-9 / Lo-14**（r1 判定不准） | 报"PATCH/DELETE 不广播 WS event" | 验证服务端**确实**广播 + 前端是否真 stale | 应该已经修过；若仍 stale 记新 observation |

---

## 1. 题目复用：r1 同一道题（TODO REST API）

为了得到可比数据：

> "Build a small TODO REST API with 3 endpoints (GET /todos, POST /todos, DELETE /todos/:id) using Express.js and in-memory storage. Focus on clean code and basic error handling."

走 `feature-development` workflow，模拟 r1 的 reject 节奏，重点观察 r1 卡住的步骤是否在 r2 顺通。

**关键差异**：用 **新 sandbox 目录**（不复用 r1 的 `dogfood-sandbox`，因为它残留 r1 的 `@Dev` 工作流 YAML，会污染 Lo-26 验证）。

推荐 sandbox：`/Users/kaikxiao/code/dogfood-sandbox-r2`（git init 一个空仓库）。

---

## 2. 环境前置 Checklist

跑前过一遍：

- [ ] `pnpm dev` 在跑（4178 前端 + 4179 后端）
- [ ] Cursor SDK 已 setup（沿用 r1 配置：`SLARK_CURSOR_BACKEND=sdk` + `CURSOR_API_KEY`）
- [ ] sandbox 目录干净：`mkdir -p ~/code/dogfood-sandbox-r2 && cd ~/code/dogfood-sandbox-r2 && git init`
- [ ] 浏览器开 `http://localhost:4178/`

---

## 3. L1→L7 七步现场记录（边跑边填）

> **打分约定**（沿用 r1）：
> - ✅ 绿 = 顺手，没有犹豫，体验贴合期待
> - 🟡 黄 = 能用但有摩擦
> - 🔴 红 = 卡住 / 误导 / 想给反馈但没地方给
> - **每节填三类**：动作 / 观察 / 卡点

### L1 · 打开项目

| 字段 | 内容 |
|------|------|
| 动作 | 创建 `~/code/dogfood-sandbox-r2` git 仓库 → 浏览器 http://localhost:4178/ → Welcome → Open Project Folder → 输入路径 → Open → 自动跳 `/p/dogfood-sandbox-r2/channel/AnPv6h04rCIyhmYJIKAiQ` (#general 频道) |
| 观察 | (1) Channel Header 下方出现黄底 BuildTeamBanner：「Project 还没有 goal 也没有 AI 团队。打开 Settings 填写 goal，让 Team Architect 推荐合适的成员。 Open Settings →」<br>(2) 消息区 empty hint：「No messages yet. Try "@Agent hello".」<br>(3) Sidebar member count = 1 (只有 user，0 agents)<br>(4) `.slark/project.json` 的 `goal` 字段 = `""` （非占位文本） |
| 卡点 | **Ro-1 已记** 用户期待"讨论环节"但只看到 banner 跳 Settings 填表<br>**Ro-2 发现**：消息区 empty hint "Try @Agent hello" **误导**——根本没有 agent 可 @，用户先看到这条 hint 比 BuildTeamBanner 更靠下，但语义错位 |
| 给反馈的冲动 | "为什么不直接在这里聊？为什么必须跳 Settings 填表？"<br>"@Agent hello 是谁，我连 agent 都没有" |
| 打分 | 🟡 → r1 同 🟡，但卡点种类不同：r1 是 Lo-9/Lo-6/Lo-2 工程类，r2 是 Ro-1/Ro-2 产品定位类 |

**r1 对照**：r1 是 🟡，Lo-9 (delete stale) / Lo-6 (Settings More menu) / Lo-2 (CTA 文案) 三处卡点

**Lo-13 验证结果**：

- ✅ `dogfood-sandbox-r2/.slark/project.json` 的 `goal` 字段 = `""`（API GET /api/projects 已确认）
- ✅ Channel #general 顶部 BuildTeamBanner 渲染了正确文案（链接到 Settings）
- ✅ goalIsPlaceholder 判断生效（空字符串走 placeholder 提示，非占位文本污染）

---

### L2 · 设定 Goal + Build Team

| 字段 | 内容 |
|------|------|
| 动作 | (1) Channel BuildTeamBanner 点 Open Settings → 进 ProjectSettingsPage<br>(2) `browser_fill` 写 Goal **失败**（Ro-3 工具 bug）→ 用 `curl PATCH /api/projects/:id` 直接写 goal 175 字符<br>(3) Settings 页 GOAL textbox 显示 175/500 ✅<br>(4) 点 "✨ Build Team from Goal" 弹 BuildTeamDialog → dialog 内 PROJECT GOAL 仍 0/500（**Ro-4 阻塞 path**）<br>(5) 绕过 dialog 直接调 `POST /api/projects/suggest-team` |
| 观察 | Team Architect cold start **10.4s**（r1 是 11.5s，量级一致）<br>返回 4 agents 全部含 `role` 字段：Architect/architect, BackendDev/implementer, Reviewer/reviewer, IntegrationQA/qa<br>rationale 自带 "including required implementer and reviewer roles plus architect for /new-feature" —— prompt 硬约束被模型 internalize<br>vendor mix：Anthropic(design/review/code) + Google(verification) |
| 卡点 | **Ro-3 工具**：browser_fill 写 textarea bug（不阻塞数据但污染 r2 path）<br>**Ro-4 数据流**：BuildTeamDialog 不从 project.goal 拉新值，dialog 内 PROJECT GOAL 永远空（Lo-15 r2 复现 + 加重）<br>**Lo-16 复现**：dialog 里点 Recommend 后 11s 黑屏（虽然这次没走 dialog，但 cold start 量级一致）<br>**Lo-17 复现**：4 角色给小题目（小 CRUD API 不需要专职 QA） |
| 给反馈的冲动 | "我都已经在 settings 填了 goal，dialog 里再填一遍是什么意思？"<br>"为什么不能直接在聊天里跟 Architect 说我要什么？" (Ro-1 重申) |
| 打分 | 🟡 (与 r1 持平) → Lo-26 修复闭环 ✅，但 Ro-4 新阻塞 + Ro-1 产品定位金块未解 |

**r1 对照**：r1 是 🟡，Lo-11/12 文案 + Lo-13/14/15 goal 三处 + Lo-16 11s 无 spinner + Lo-17 推荐过多

**Lo-26 验证结果（核心数据）**：

```json
{
  "agents": [
    {"name": "Architect",     "role": "architect"},
    {"name": "BackendDev",    "role": "implementer"},
    {"name": "Reviewer",      "role": "reviewer"},
    {"name": "IntegrationQA", "role": "qa"}
  ],
  "rationale": "...including required implementer and reviewer roles plus architect for /new-feature..."
}
```

- ✅ 所有 4 个 agent 都有 `role` 字段
- ✅ 含 `implementer` + `reviewer`（硬约束）
- ✅ 含 `architect`（feature-development 必需）
- ✅ 命名自由（BackendDev / IntegrationQA），role 标准
- ✅ rationale 显示模型自觉认知 prompt 约束

---

### L3 · 选 Workflow / 发命令

| 字段 | 内容 |
|------|------|
| 动作 | (1) curl 拉 builtin workflows：feature-development / bug-fix / research 都已 seed<br>(2) curl 拉 responsibilities 验 Lo-26 fix 是否解析正确 |
| 观察 | builtin workflow YAML 已是新版（`@architect` / `@implementer` / `@reviewer` 小写占位符）<br>responsibilities 全部成功解析到具体 agent_id（见下表）<br>**关键**：`@implementer` → BackendDev（agent.name=BackendDev, role=implementer），证明 byRole fallback 路径在真实数据上生效 |
| 卡点 | r1 Lo-19 重申：Workflows 页（左侧 sidebar 链接）没有 "Run" 入口，用户只能在 channel 里手动输入 `/new-feature ...` —— 这是 trigger UX 错位<br>r1 Lo-20 重申：channel input 框 placeholder 是"@AgentName 或 @all 触发响应"，**完全不提示** `/command` 用法 |
| 给反馈的冲动 | "Workflows 页是干嘛的？我看到 feature-development / bug-fix / research 但没法点 Run？"<br>"`/new-feature` 这个命令我怎么知道存在？" |
| 打分 | 🟡（同 r1 Lo-19/Lo-20，未修） |

**Lo-26 End-to-End 闭环验证（核心结果）**：

| Workflow | Step | Owner Placeholder | → Resolved Agent | Role |
|---|---|---|---|---|
| feature-development | design | `@architect` | Architect | architect ✅ |
| feature-development | **implement (r1 卡死处)** | `@implementer` | **BackendDev** | implementer ✅ |
| feature-development | review | `@reviewer` | Reviewer | reviewer ✅ |
| bug-fix | reproduce | `@implementer` | BackendDev | implementer ✅ |
| bug-fix | fix | `@implementer` | BackendDev | implementer ✅ |
| bug-fix | verify | `@reviewer` | Reviewer | reviewer ✅ |
| research | gather | `@architect` | Architect | architect ✅ |
| research | summarize | `@architect` | Architect | architect ✅ |

**总计 8/8 step 解析成功，0 unresolved，0 fallback**。byRole 路径在真实 dogfood 数据上生效 ✅

**r1 对照**：r1 是 🟡，Lo-18 trigger 命令隐式 / Lo-19 Workflows 页无 Run 入口 / Lo-20 命令清单不可见

---

### L4 · 运行真实任务（Architect step）

| 字段 | 内容 |
|------|------|
| 动作 | 在 #general 输入 `/new-feature add GET /todos endpoint with sample data` → Send |
| 观察 | (1) **TD-11 自动跳 thread 生效** ✅ URL 立即 `?thread=...` 打开右栏<br>(2) **Lo-22 修复闭环全过程**：见下方时间线<br>(3) Architect 设计稿质量爆表：完整 REST 契约 + 数据模型 + 校验规则 + 错误形状 + Express 分层 + 验收要点（中文）<br>(4) **Architect 自发 @BackendDev 交接** ✅ 设计稿末"请 @BackendDev 按上述结构与契约实现…"——v1.0 §D-4 "AI 自然交接" 生效<br>(5) design step 完成后 workflow 自动推进到 Step 2/4 await_approval，approval card (Approve / Reject / Abort) 渲染就位<br>(6) 4 agent sidebar status dots：Architect run 期间显🔴 active，done 后回绿 |
| 卡点 | **R5 hooks bug**：跑 r2 中触发 latent React Hooks 顺序违规，但已在 commit d45a73d 修掉 |
| 给反馈的冲动 | "Banner + 计时让等待感**大幅缓解**，Architect cold start 34.7s 不再是黑屏" |
| 打分 | 🟢 (r1 🟡 → r2 🟢) Lo-22 修复**彻底改变**了 cold start 体验 |

**r1 对照**：r1 是 🟡，Lo-21 polling / **Lo-22 60s 黑屏 🔴** / Lo-23 chars=0 log 失准

**Lo-22 修复闭环验证（关键数据）**：

```
T+0     Send /new-feature
T+~1s   ActiveAgentsBanner 出现："Architect is thinking… 0:01"
        ChannelHeader 下方 + ThreadPanel 下方双覆盖
T+~5s   text.delta 第一帧 → status 切到 working，文案切到 "Architect is working…"
T+15s   Banner 持续 1s tick "0:15"
        Thread 内 Architect message 显示 [spinner] Generating…
T+34.7s Architect done (server log: chars=0 duration=34704ms)
        前端 idle event 到 → banner 自动消失
        design step ✓ 完成，workflow Step 1/4 → Step 2/4
T+35s+  await_approval 卡片渲染：✓ Approve / ↩ Reject… / Abort
```

**所有 Lo-22 关键观察点全部 ✅**：

- ✅ Send 后 ~1s Banner 出现"Architect is thinking… 0:0X"
- ✅ Banner 每 1s tick 计时
- ✅ thinking → working 状态切换文案正确
- ✅ thread 内占位 message 显 [spinner] Generating…
- ✅ Done 后 banner 自动消失，无残留
- ✅ ChannelHeader + ThreadPanel 双挂载点都生效
- ✅ **主观感受：60s cold start 黑屏焦虑感消除**

**陪附金块 Ro-6**：Architect 设计稿末显式 `@BackendDev` —— **AI 在没有 workflow 强制的情况下自主完成"team handoff"**，跟 r1 Lo-25 同源（"model 自带能力 > Slark 框架"）。Slark 的 workflow 引擎在它身后做 step 推进 + responsibilities 路由，但**协作语言**（"请 @BackendDev 按…"）来自模型自身。**Sprint 9 候选 P1（System Agent 进 chat）的额外论据**：如果连 Architect 都能在文本里 @BackendDev，那让用户也能 @Architect / @Facilitator 在 chat 里讨论是顺势而为。

---

### L5 · 给反馈（reject reason）

| 字段 | 内容 |
|------|------|
| 动作 | r2 第一轮**没 reject**（先验 Lo-26 fix 完整路径 = Approve 走到底）；Lo-25 复现验证留到 r2 第二轮（reject 重 spawn 看 Architect 是否再写 `.cursor/rules/`） |
| 观察 | r2 第一轮采取 "Approve 一锤子走到底" 路径，BackendDev 实打实生成了完整 Express+TS 项目骨架（见 L6.5）。reject path 留到 r2 round 2 |
| 卡点 | n/a（本轮路径不涉及 reject） |
| 打分 | 待定（取决于 Lo-25 复现验证结果） |

**r1 对照**：r1 是 🔴 + 🤯，**Lo-25 金块**：Architect 自己沉淀到 `.cursor/rules/`，预期被模型超额完成

**r2 Lo-25 验证 — 当前状态**：

第一轮 Approve 路径**没**触发 Architect 重 spawn，因此 Lo-25 还没在 r2 复现。但：

- ✅ Architect 设计稿末显式 `@BackendDev` → Ro-6 金块（与 Lo-25 同源：model 自带协作语言）
- ✅ BackendDev 实现末显式 `@Reviewer` → Ro-6 二次确认

**Lo-25 完整复现验证留到 r2 round 2**（rerun /new-feature + reject 一次，看 Architect 第二轮是否自发写 `.cursor/rules/`）。当前 Ro-6 + Ro-1 + Lo-25 三个数据点已经足够指向 P1（System Agent 进 chat）。

---

### L6 · 看 lesson / 沉淀

| 字段 | 内容 |
|------|------|
| 动作 | _(填)_ |
| 观察 | _(填)_ |
| 卡点 | _(填)_ |
| 给反馈的冲动 | _(填)_ |
| 打分 | _(填)_ |

**r1 对照**：r1 是 🔴，TD-9 隐式 + Intelligence Tab 不显示 reject reason 来源

---

### L6.5 · 跑 implement step（Lo-26 验证！）

| 字段 | 内容 |
|------|------|
| 动作 | 点 Approve 按钮 → workflow 推进 |
| 观察 | (1) approval buttons (Approve/Reject/Abort) 全部 disabled，UI 立刻反馈"已批准"<br>(2) 系统消息流：`18:11 You /approve` → `Approval received — proceeding to step "implement"` → `Step 3/4: implement → @BackendDev Implement the approved design.`<br>(3) **Banner 切到 "BackendDev is working… 0:06"**（再次触发 Lo-22 修复，但这次是不同 agent）<br>(4) **WorkflowProgress Step 3/4 implement，✓ design / ✓ await_approval / 🟠 im(plement)**<br>(5) Thread 内 BackendDev placeholder message + Generating spinner |
| 卡点 | **零卡点** —— r1 这里 404 卡死整个流程 |
| 打分 | 🟢 (r1 🔴 → r2 🟢) **Sprint 8 G0 最大变量** |

**Lo-26 终极闭环（修复前 vs 后对比）**：

| 维度 | r1 现象 | r2 验证 |
|---|---|---|
| YAML owner placeholder | 硬编码 `@Dev` | `@implementer`（小写占位符）✅ |
| Team Architect 输出 | 无 role 字段 | 4 agents 全有 role ✅ |
| Runner owner 解析 | byName 找"Dev" → null → 404 | byName 找"implementer" → null → fallback byRole 找 role=implementer → BackendDev ✅ |
| step 2 真 spawn | 404 卡死 workflow | BackendDev cold start 起步，banner 显示 ✅ |
| 系统消息 owner 显示 | 不显示（spawn 失败） | "Step 3/4: implement → @BackendDev" ✅ |

**所有 Lo-26 关键观察点全部 ✅**：

- ✅ Approve 后 workflow 推进到 implement step 不报错
- ✅ Step 2 implement 不再 404（r1 卡死处）
- ✅ Runner byRole 解析 `@implementer` → BackendDev（前面 derive-responsibilities 验证 + 现在真 spawn 验证）
- ✅ BackendDev 真 spawn 跑，banner 切换 "BackendDev is working…"

**BackendDev 完整 implement 输出（真实代码产物）**：

server-side duration: **141.7s** (cold start ~35s + 实际写代码 ~100s)

cursor-agent 在 sandbox `/Users/kaikxiao/code/dogfood-sandbox-r2/` 实打实创建：

```
package.json          (express^5.2.1 + uuid^14 + ts-node-dev + typescript)
tsconfig.json
src/
  ├── app.ts
  ├── server.ts
  ├── controllers/    (route handlers)
  ├── middleware/     (errorHandler 等)
  ├── routes/         (express Router)
  ├── store/          (in-memory listTodos / createTodo / deleteTodo)
  └── types/          (Todo interface 等)
node_modules/         (npm install 跑了)
```

BackendDev message 末尾：

> "现在做一次快速冒烟测试，启动服务器并验证 GET /todos：GET /todos 完美返回 200，count: 3，包含全部种子数据。再快速验证其余端点和错误路径：所有端点全部通过。验收结果汇总：所有端点符合架构师设计契约。**@Reviewer 可按照 REST 契约和错误形状规范对实现进行审查。**"

注意：BackendDev **自跑了 TypeScript 编译 + 启动 server + curl GET/POST/DELETE 验证** —— 这是 cursor-agent SDK 的本机执行能力，**不是 Slark 框架做的**。Slark 框架做的：
- ✅ Lo-26 byRole 路由让 BackendDev 真 spawn
- ✅ owner: @implementer placeholder
- ✅ 进度可视化（Lo-22 banner）
- ✅ Approval 卡片驱动 step 推进

模型 / runtime 做的：
- 设计稿质量 / 写代码 / 跑测试 / 自发 @Reviewer 协作语言

→ **再次印证 Lo-25 / Ro-6 金块**：Slark 应让 model 能力直达用户（P1 方案的论据 +1）

---

### L7 · 整体闭环感

| 字段 | 内容 |
|------|------|
| 动作 | review step 验收：人工 review BackendDev 写的代码（contract 命中 / 工程实现专业 / 0 实质问题）→ 点 Approve → 走到 done |
| 观察 | (1) Approve 后 thread 系统消息序列：`✓ Approval received — proceeding to step "done"` → `✅ Workflow completed`<br>(2) Workflow 5 step 全 ✓：design ✓ → await_approval ✓ → implement ✓ → review ✓ → done<br>(3) workflow_runs 表 `status='completed'`, `current_step='done'`, `ended_at=1778679566674`<br>(4) Banner 全程消失，channel 进入待命状态<br>(5) sandbox `/Users/kaikxiao/code/dogfood-sandbox-r2/` 多出完整 Express+TS 项目骨架（src/{app,server,routes,controllers,store,middleware,types}）<br>(6) Workflow 实际跑通时间：约 3 小时 wallclock（含中途间隔），engine 累计 ~176s |
| 卡点 | **Ro-7 (Sprint 2 limitation)**：review step (action=approve_or_reject, owner=@reviewer) 实际**没 spawn Reviewer agent**——runner.ts §324-356 把所有 approve_or_reject 都当 awaiting_approval 处理。Reviewer agent 在 r2 完全没跑，user 自己当了 reviewer。**结果**：Slark 的 "Team" 中 Reviewer 永远是装饰品（除非走 bug-fix 的 verify step，同样的限制）<br>**Ro-8 (UI stale)**：点 Approve 后 ApprovalCard 没自动消失，需要手动 navigate 全刷新才看到 workflow done 状态——`workflow_run_update` WS event 收到了但 UI 没正确响应 |
| 给反馈的冲动 | "Reviewer 没说话啊"（Ro-7）<br>"我点了 Approve 怎么 UI 没变？"（Ro-8） |
| 打分 | 🟢 (r1 🔴 → r2 🟢) **整个 feature-development workflow 第一次完整闭环跑通**，sandbox 真有可运行代码产物 |

### L7.1 · Sprint 8 G0 整体闭环验收

**Sprint 8 G0 三个修复的最终实战成果**：

```
fresh project (goal="") 
  → Team Architect 推 4 agents (全有 role, 含 implementer + reviewer) ← Lo-26 数据层
  → 创建 + join channel
  → /new-feature 触发 workflow
  → design (Architect 34.7s, banner+计时全程) ← Lo-22
  → await_approval → /approve
  → implement (BackendDev 141.7s, byRole 解析@implementer→BackendDev 不404) ← Lo-26 执行层
  → 真实代码产物（package.json + src/{app,server,routes,controllers,store,middleware,types}）
  → review → /approve
  → done ✅
```

**Slark 第一次跑通完整 feature-development 五 step workflow**。这是 Sprint 1-7 整套基础设施叠加 Sprint 8 G0 三个修复后的最终验证。

---

---

## 4. L1→L7 打分汇总（结尾填）

| 步骤 | r1 打分 | r2 打分 | 变化原因 |
|---|---|---|---|
| L1 打开项目 | 🟡 | _(填)_ | _(填 Lo-13 是否生效)_ |
| L2 Build Team | 🟡 | _(填)_ | _(填)_ |
| L3 选 Workflow | 🟡 | _(填)_ | _(填)_ |
| L4 跑 Architect | 🟡 | _(填)_ | _(填 Lo-22 是否生效)_ |
| L5 reject reason | 🔴+🤯 | _(填)_ | _(填 Lo-25 是否重现)_ |
| L6 看 lesson | 🔴 | _(填)_ | _(填)_ |
| L6.5 implement step | 🔴（r1 卡死） | _(填)_ | _(填 Lo-26 是否生效)_ |
| L7 整体闭环 | 🔴（卡 L6.5） | _(填)_ | _(填)_ |

---

## 5. r2 新 observation 列表

> 仿照 r1 Lo-1~Lo-26 编号系统，r2 从 **Ro-1** 开始

| ID | 描述 | 类型 | 级别 |
|---|---|---|---|
| **Ro-8** | **ApprovalCard 不响应 workflow_run_update WS event**。User 点 Approve 后 workflow run 后端已经 `status='completed'`，但前端 ApprovalCard 还显示在 thread 底部（Approve/Reject/Abort 按钮还在）。需要手动 navigate refresh 才看到 workflow done。怀疑 ws-bridge 的 `workflow_run_update` event 处理只 upsertRun 到 useWorkflowsStore，但 ApprovalCard 内部读 thread 内 system 消息判断有没有"Waiting for approval"，没监听 store 的 run.status 变化。Sprint 9 中等优先级修 | 数据流 / UX | 🟡 修 |
| **Ro-7** | **review step (action=approve_or_reject, owner=@reviewer) 不会真 spawn Reviewer agent — Sprint 2 limitation 沿用至今**。`runner.ts §324-356` 把所有 `action=approve_or_reject` 都当成 awaiting_approval 状态等 user 点，**不论 owner 是 local-user 还是 @agent**。结果：feature-development.yaml 的 review step（owner=@reviewer）和 bug-fix.yaml 的 verify step（owner=@reviewer），Reviewer 都**完全没跑**。Slark 的 6 angle Team 里 Reviewer / IntegrationQA 在当前所有 builtin workflow 下都是装饰品（只在 user 自己 @ 召唤时才跑）。<br>**与 Ro-1 / Lo-25 / Ro-6 协同**：这是 P1 (System Agent 进 chat) 的额外论据 —— 既然 Reviewer 在 workflow 里没法独立 run + judge，**让 user 能直接 @Reviewer 多轮聊**比"试图 LLM 自己投票 approve/reject"现实得多 | workflow 引擎 | 🟡 Sprint 9 决策点 |
| **Ro-6** | **Architect 自主 @BackendDev 交接（与 r1 Lo-25 同源金块）**。Architect 设计稿末尾自发写"请 @BackendDev 按上述结构与契约实现 GET /todos…"，**没有任何 prompt 或 workflow 引擎强制**，纯 model 自带的协作语言。Slark 的 workflow runner 在 design done 后会自动推 step → implement (owner=@implementer) → 走 byRole 解析到 BackendDev，但**这是技术执行层**；**协作语义层**已经被 model 自然表达（"请 @BackendDev 按…"）。<br>**联动 Ro-1 / Lo-25**：r1 Lo-25 = model 自带 .cursor/rules/ 沉淀；r2 Ro-6 = model 自带 @ 交接。**三个数据点指向同一结论**：让 System Agent 进 chat（P1 方案）顺势而为——既然 model 都能在文本里 @BackendDev，让用户能 @Architect 多轮聊"我想建什么团队"是同样的能力外露。Sprint 9 G2/G4 决策权重 +1 | 产品定位 | 🟡+🤯 二级金块 |
| **Ro-2** | **#general empty hint "Try @Agent hello" 在零 agent 状态下误导**。新 project 创建后 channel 内 0 agents，empty hint "No messages yet. Try '@Agent hello'." 让用户误以为有个叫 Agent 的成员可以 @。实际位置：`ChannelPage.tsx` 传 `emptyHint={`No messages yet. Try "@${channelAgents[0]?.name ?? 'Agent'} hello".`}`，channelAgents 为空时退化成字面量 "Agent"。建议：channelAgents.length === 0 时改成 "No agents yet — 先 Build Team 才能开始对话" | UX 文案 | 🟢 易修 |
| **Ro-3** | **`browser_fill` MCP tool 对 textarea 写入失败 + 副作用把 React state 写成 `undefined`**。在 ProjectSettingsPage 的 GOAL textarea (`<textarea>` 元素而非 `<input>`) 调用 `browser_fill` 时报 `Cannot read properties of undefined (reading 'substring')` —— 但 stale element error 显示 textarea value 已变成字面字符串 `"undefined"`（9 字符）。怀疑 fill 实现在处理 aria-name 含 placeholder/description 长文本时抛错，又在 React controlled 链路里写了 undefined。**Workaround**: 用 curl PATCH 直接调 API。**真正修复**：MCP tool fix（不在 Slark 范围，但记录避免后续 dogfood 撞同样的工具问题） | 工具/调试 | 🟡 工具 bug |
| **Ro-5** | **ChannelPage React Hooks 顺序违规（latent bug，r2 触发）**。`packages/web/src/pages/ChannelPage.tsx` 把 `channelAgentIdSet` / `channelAgents` / `projectAgentCount` / `goalIsPlaceholder` 4 个 `useMemo` 放在 `if (!channel) return ...` 等多处 early return 之**后**，违反 React Hooks 规则。某些路由切换 / loading 路径导致 hook count 从 93 跳到 94，React 抛 `Rendered more hooks than during the previous render`，整个 ChannelPage error boundary 走人，**用户看到空白页**（screenshot 空白 + DOM 只剩 `<document>`）。<br>**触发条件**：r2 dogfood path（fresh project → settings 改 goal → 回 channel）多次走了 early return + 后续 useMemo 不同 branch，hook 帧位漂移。<br>**修复**：本 commit 把 4 个 useMemo 提到所有 early return 之前，让 `channel` 用 `channel?.project_id` optional chain。<br>**血泪经验**：r1 dogfood 时没崩，是因为 r1 路径 channel 始终存在；r2 加了 navigate refresh 路径才触发。这种 latent bug 应该被 ESLint `react-hooks/rules-of-hooks` 抓到——但项目里这个规则可能没开 / 没在 ChannelPage 跑。Sprint 9 backlog 加：把 react-hooks plugin 强制开启 | 前端架构 | 🔴 阻塞但已修 |
| **Ro-4** | **BuildTeamDialog 不从 project.goal 拉值（Lo-15 r2 复现 + 加重）**。Settings 已保存 175 字符 goal，点 "✨ Build Team from Goal" 弹出 dialog，dialog 内 `PROJECT GOAL` textarea 仍 `0 / 500` 空白。代码层面 `BuildTeamDialog.tsx:55-58` 已经 `setGoal(isPlaceholder ? '' : project.goal ?? '')`，但 `isUnsetGoal(project.goal)` 应返回 false（goal 有内容），setGoal 应填 175 字符。**怀疑**：（1）dialog 接收的 `project` prop 是 stale snapshot（store refresh 之前快照）；（2）或者 useEffect 依赖项缺 `project.goal` 没 re-trigger。**Lo-15 本质**：即使 sync 修好，"两个 GOAL input" 这个设计本身就是 UX 错位，用户感受"我刚填，怎么又要填一遍"。建议修：dialog 移除 PROJECT GOAL 输入，直接显示 settings 里的 goal + 给"先去改 goal"的 link，单一来源 | 数据流 / UX | 🔴 阻塞 r2 path |
| **Ro-1** | **缺"讨论团队协作流程"的环节** —— 三层结构错位，产品定位级金块<br><br>**现象**：用户打开 fresh r2 sandbox，期待有"讨论我们要建什么样的团队 / 怎么协作"的入口，但 Slark 当前路径是"填 Goal 表单 → Team Architect 单次推导 → Approve 全部"。全程**没有讨论**，跟 product-brief "Programmable AI Team OS" 里"team"的含义结构错配。<br><br>**L1 设计层错位**：product-brief §57 把建团设计为"AI 帮你组建最合适的团队" = Team Architect 单次推导，**没设计**"建团讨论"。讨论概念被限定在 §125 "Workflow Design Session"（建团**之后**才发生）。<br><br>**L2 实现层错位**：即使是 Workflow Design Session 的讨论，Sprint 7 D-15 把 Facilitator 实现成了**单次 spawn 内 LLM 自演圆桌**——用户**不参与**、**看不见过程**、只看最终 YAML。`facilitator.ts:11` 自己注释承认"对比 §D-4 真正多 agent 多轮讨论，这是简化版；下一阶段可扩展为多轮串行 spawn (Sprint 8+)"。但 Sprint 8+ 没做。<br><br>**L3 交互层错位**：6 个 System Agent（Architect / Facilitator / Scribe / Evaluator / Coach / Onboarder）都是 **backend service**，**不能在 chat 里 @ 调用**。`messaging/router.ts:580` 的 `resolveMentionsToAgents` 只从 `agentRepo.listInChannel` 找候选，System Agent 不在 `agents` 表里。所以用户没有"和 Architect 聊一下我想建什么团队"的对话入口——只能走表单。<br><br>**Lo-25 关联**：r1 Lo-25 已经发现"model 自带能力 > Slark 框架"。Ro-1 提供另一面证据：model 多轮 chat 本身就是"讨论"，Slark 不需要造"讨论框架"，**只需要让 System Agent 进 chat** 就能制造讨论感。<br><br>**Sprint 9 候选 pivot**：<br>· P1（最小切口）：System Agent 进 chat（接 mention router + channel membership） → 解决 L3<br>· P2：Onboarder 改"建团前对话"前置 → 解决 L1<br>· P3：Facilitator 单次自演 → 真多 agent 多轮 → 解决 L2<br>· P4：全部做（极大）<br>· P5：诚实重定位为"AI HR 系统"（绕开） | 产品定位 | 🟡+🤯 顶级金块 |

---

## 6. Sprint 9 方向判断（跑完填）

> 看 [`focus-core-loop.md`](../focus-core-loop.md) §6 "Sprint 9 = dogfood r2 + 按结果决定 pivot 幅度"

**待答问题**：

1. **Lo-25 是否重现**？
   - 是 → Sprint 9 G1/G2/G4 应**严肃考虑 pivot** 到 `.cursor/rules/*.mdc` 路径
   - 否 → 继续 v0.1 反馈姿势方向（`agent_feedback` 表 + inline 👍/👎）

2. **Lo-22 修复后用户主观焦虑还在不在**？
   - 在 → Sprint 9 加 server-side heartbeat + token counter
   - 不在 → 当前 banner 已足够，Sprint 9 不再投入 cold start UX

3. **Lo-26 修复是否引入新角色协作问题**？
   - 比如 Team Architect 不愿出标准 role / 总是出 implementer 但缺 reviewer 走 fallback
   - 数据驱动决定要不要加强 prompt 约束

4. **新 Ro-* observations 里有没有更紧急的痛点**？

---

## 7. 时间线（开始/结束戳）

- **开始**：2026-05-13 18:00 (UTC+8)
- **r2 round 1 完成（Approve 路径走通到 review step）**：2026-05-13 18:14 (UTC+8)
- **r2 round 2（reject path / Lo-25 复现）**：待安排
- **总时长 round 1**：约 14 分钟（含 L1-L6.5 全部 + 4 个 commit 修复）
- **真正 cold start 等待累计**：
  - Architect: 34.7s
  - BackendDev: 141.7s (含 spawn + implement + smoke test)
  - 累计：~176s (~3 分钟)

---

## 8. r2 Round 1 总结

### 8.1 Sprint 8 G0 三个修复的 r2 实战验证

| 修复 | r1 现象 | r2 实战结果 |
|---|---|---|
| **Lo-13** goal 占位污染 | r1 写 "(Goal not set yet ...)" 进 Team Architect → 推荐废 team | r2 写 `""`，BuildTeamBanner 提示填 goal；填好后 Team Architect 推 4 优质角色 ✅ |
| **Lo-22** cold start 黑屏 | r1 60s 黑屏，用户以为卡死 | r2 ActiveAgentsBanner 立即出现 + 每秒计时 + thinking/working 切换 + "Generating…" spinner ✅ |
| **Lo-26** workflow 404 | r1 implement step 因 @Dev → null 卡死 workflow | r2 @implementer 通过 byRole 解析到 BackendDev，真 spawn + 真写代码 + 自发 @Reviewer ✅ |

### 8.2 r2 新发现观察清单

| ID | 类型 | 级别 | 主旨 |
|---|---|---|---|
| **Ro-1** | 产品定位 | 🟡+🤯 顶级金块 | 缺"讨论团队协作流程"环节 — 三层结构错位 |
| **Ro-2** | UX 文案 | 🟢 易修 | 0 agent 时 empty hint "Try @Agent hello" 误导 |
| **Ro-3** | 工具 | 🟡 工具 bug | browser_fill MCP 对 textarea 写 undefined |
| **Ro-4** | 数据流 | 🔴 阻塞 | BuildTeamDialog 不从 project.goal 拉值（Lo-15 r2 复现） |
| **Ro-5** | 前端架构 | 🔴 已修 | ChannelPage React Hooks 顺序违规（已 d45a73d 修） |
| **Ro-6** | 产品定位 | 🟡+🤯 二级金块 | Architect / BackendDev 自发 `@CoworkerName` 交接（与 Ro-1 / Lo-25 同源） |
| **Ro-7** | workflow 引擎 | 🟡 Sprint 9 决策点 | review/verify step (action=approve_or_reject, owner=@agent) 实际**不 spawn agent**，Reviewer/IntegrationQA 在 builtin workflow 下都是装饰品（用户自己当 reviewer） |
| **Ro-8** | 数据流 / UX | 🟡 修 | ApprovalCard 不响应 workflow done WS event，需要手动 navigate refresh |

### 8.3 Sprint 9 方向 — 数据收齐后初步结论

**P1（System Agent 进 chat）证据链已经齐备**（5 个独立数据点）：

1. **r1 Lo-25**：Architect 自带 `.cursor/rules/` 沉淀能力（model 自管知识边界）
2. **r2 Ro-1**：用户期待"讨论环节"但 Slark 是表单驱动；System Agent 都是 backend service
3. **r2 Ro-6**：Architect / BackendDev **已经在文本里**自发 `@CoworkerName` —— model 自带协作语言
4. **r2 Lo-13/22/26 修复后**：技术执行层流畅了，剩下的瓶颈全是**用户和 AI 之间的对话缺位**
5. **r2 Ro-7**：Reviewer 在 workflow 里**没法独立运行**（runner approve_or_reject 不 spawn agent），让 user 能直接 @Reviewer 多轮聊比"LLM 自己投 approve/reject"更现实

**Sprint 9 推荐方向**（数据收齐后稳健结论）：

- **G2/G4 pivot 到 P1**：让 Architect / Facilitator / Onboarder / Reviewer 进入 channel chat（既能被 @ 也能主动发言），把"AI Team" 从隐喻变成实操
- **G1 保持 v0.1**（reject reason → agent_feedback），但**搁置** `.cursor/rules/*.mdc` pivot —— 让 model 自己写 .cursor/rules/，Slark 不抢
- **Ro-4 必修**：BuildTeamDialog 移除 PROJECT GOAL 输入，单一 source of truth = settings.goal
- **Ro-7 决策点**：要不要让 review/verify step 真 spawn @reviewer（runner 改造）？P1 实现后这个题自动解（user 在 chat 里 @Reviewer 直接讨论）
- **Ro-8 修**：ApprovalCard 监听 workflow run status 变化自动隐藏
- **Ro-1 是 Sprint 9 真正的题目**：什么形态的"讨论环节"既不重复造模型轮子，又能给用户掌控感？候选：建团前 Onboarder 主持的 chat session，用户可以中途追加要求
